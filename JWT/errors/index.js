const CustomAPIError = require("./custom-error");
const Unauthenticated = require("./unauthenticated");
const BadRequest = require("./bad-requested");

module.exports = {
  CustomAPIError,
  Unauthenticated,
  BadRequest,
};
